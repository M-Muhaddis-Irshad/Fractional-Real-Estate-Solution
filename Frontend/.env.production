# Deployed backend (Vercel) — REST lives on this host.
NEXT_PUBLIC_API_URL=https://crypto-fractional-backend.vercel.app
API_PROXY_TARGET=https://crypto-fractional-backend.vercel.app

# Standalone socket server (Render) — real-time updates. Replace with the
# deployed Render URL after creating the service (e.g. https://flux-socket.onrender.com).
NEXT_PUBLIC_SOCKET_URL=https://crypto-fraction-backend.onrender.com/

